% Pneuma Blueprint preamble
% Required packages for Lean Blueprint integration
\documentclass[a4paper]{report}

\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{hyperref}
\usepackage{xcolor}

% Blueprint-specific commands
% These are provided by the leanblueprint package when building with
% the Blueprint tool. For standalone compilation, we define stubs.
\providecommand{\lean}[1]{}
\providecommand{\leanok}{}
\providecommand{\uses}[1]{}
